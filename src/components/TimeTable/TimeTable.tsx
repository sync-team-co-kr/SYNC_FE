import { TimeTableProps } from './TimeTable.types';

const TimeTable = ({
  title,
  description,
  startTime,
  endTime,
  images,
  projectId,
  parentTaskId,
  status,
}: TimeTableProps) => {
  return <div>타임테이블</div>;
};

export { TimeTable };
